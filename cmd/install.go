package cmd

// InstallCmd restows public then private into the target. Stub until
// feature 2 lands.
type InstallCmd struct{}

func (c *InstallCmd) Run(cli *CLI) error {
	return notImplemented("install")
}
