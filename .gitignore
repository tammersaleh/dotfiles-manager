# Binary
/dfm

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Local Claude Code config (user-specific, not shared)
CLAUDE.local.md
.claude/CLAUDE.local.md

# Untracked scratch space for work orders. Ignored, not just untracked, so a
# stray `git add -A` cannot commit them.
/bugs/
/todo/
